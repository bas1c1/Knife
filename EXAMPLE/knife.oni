def StartListening(response, url) {
	stack = new_stack()
	append("stack", response)
	append("stack", url)

	im = import_cs_class("Knife", "Knife")
	ins = get_cs_class_inst("Knife", "Knife")
	exec_cs_method(im, ins, "_listener", stack)
}

to_import("StartListening")
